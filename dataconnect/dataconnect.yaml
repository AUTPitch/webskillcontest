specVersion: "v1"
serviceId: "webskillcontest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webskillcontest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
